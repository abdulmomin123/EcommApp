import { SpinnerContainer, SpinnerOverlay } from '../styles/Spinner';

const Spinner = () => {
  return (
    <SpinnerOverlay>
      <SpinnerContainer />
    </SpinnerOverlay>
  );
};

export default Spinner;
